const batteryBatches = [4, 5, 3, 4, 4, 6, 5];

const totalBatteries = batteryBatches.reduce((lastBat, currentBat) => lastBat + currentBat, 0)


































// const totalBatteries = batteryBatches.reduce((lastBattery, currentBattery) => lastBattery + currentBattery, 0)
